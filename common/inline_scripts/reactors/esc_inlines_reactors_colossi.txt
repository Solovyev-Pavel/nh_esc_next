### inline for colossus and other similar ships' reactors

power = $POWER$

resources = {
	category = ship_components
}

prerequisites = { $PREREQUISITES$ }
size_restriction = {
	# vanilla ship classes
	colossus star_eater }
component_set = "power_core"
